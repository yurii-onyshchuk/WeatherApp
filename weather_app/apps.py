from django.apps import AppConfig


class WeatherAppConfig(AppConfig):
    """Configuration class for the 'weather_app' app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'weather_app'
